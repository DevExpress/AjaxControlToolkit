<!DOCTYPE html>
<html>
    <head>
        @*<title>@Page.Title</title>
        @RenderSection("head", required: false)*@
    </head>
    <body>
        @(1+2)
        @RenderBody()
    </body>
</html>
