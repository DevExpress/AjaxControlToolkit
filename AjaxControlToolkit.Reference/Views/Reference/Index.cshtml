@model string[]

@{
    Layout = "~/Views/_Layout.cshtml";
}

<ul>
    @foreach(var typeName in Model) {
        <li>@Html.ActionLink(typeName, "Markup", "Reference", new { typeName = typeName }, null)</li>
    }
</ul>
